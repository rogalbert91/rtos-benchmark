# Skip subdirectory processing for Pico SDK builds (handled in freertos_pico2_sdk.cmake)
if(NOT PICO_SDK_BUILD)
    add_subdirectory(arch)
    add_subdirectory(timer)
    add_subdirectory(boards)
endif()
